---
/*
 * @Author: Antigravity AI
 * @Date: 2026-09-07
 * @FilePath: /src/components/views/GroupProfileEnvironment.astro
 * @Description: 希瑪微笑矯視中心 - 中心環境雙層聯動輪播組件 (100% 官方原始文案、結構與經典原版導航位置)
 */
import { getLangFromUrl } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const isCn = lang === "zh-cn";

// 5 大中心環境實景輪播圖資產 (蔡司全套設備與旗艦門市 - 現代無損 WebP)
const slides = [
  {
    src: "/assets/images/group-profile/env-01.webp",
    alt: isCn ? "香港希玛微笑矫视中心中环中建大厦旗舰店舒适候诊空间" : "香港希瑪微笑矯視中心中環中建大廈旗艦店舒適候診空間",
  },
  {
    src: "/assets/images/group-profile/env-02.webp",
    alt: isCn ? "香港希玛微笑矫视中心旺角雅兰中心旗舰店独立咨询室" : "香港希瑪微笑矯視中心旺角雅蘭中心旗艦店獨立諮詢室",
  },
  {
    src: "/assets/images/group-profile/env-03.webp",
    alt: isCn ? "德国蔡司 SMILE Pro 国际高规格层流无菌手术室" : "德國蔡司 SMILE Pro 國際高規格層流無菌手術室",
  },
  {
    src: "/assets/images/group-profile/env-04.webp",
    alt: isCn ? "香港希玛微笑矫视中心 VIP 贵宾休息大堂" : "香港希瑪微笑矯視中心 VIP 貴賓休息大堂",
  },
  {
    src: "/assets/images/group-profile/env-05.webp",
    alt: isCn ? "全套德国蔡司屈光专科术前精密检验室" : "全套德國蔡司屈光專科術前精密檢驗室",
  },
];
---

<div class="gp-env-swiper-wrapper">
  {/* 雙層 Swiper 核心區塊 */}
  <div class="gp-env-carousel">
    {/* 1. 主展示大輪播圖 */}
    <div class="swiper gp-env-main-swiper" id="gpEnvMainSwiper">
      <div class="swiper-wrapper">
        {slides.map((slide) => (
          <div class="swiper-slide gp-env-main-slide">
            <img
              src={slide.src}
              alt={slide.alt}
              loading="lazy"
              decoding="async"
              class="gp-env-main-img"
            />
          </div>
        ))}
      </div>
    </div>

    {/* 2. 縮略圖導航條 */}
    <div class="swiper gp-env-thumbs-swiper" id="gpEnvThumbsSwiper">
      <div class="swiper-wrapper">
        {slides.map((slide) => (
          <div class="swiper-slide gp-env-thumb-slide">
            <img
              src={slide.src}
              alt={slide.alt}
              loading="lazy"
              decoding="async"
              class="gp-env-thumb-img"
            />
          </div>
        ))}
      </div>
    </div>

    {/* 原版左右箭頭 (位於輪播兩側，高度精確對齊原版) */}
    <button type="button" class="gp-env-nav-btn gp-env-nav-btn--prev" aria-label="上一張環境照片"></button>
    <button type="button" class="gp-env-nav-btn gp-env-nav-btn--next" aria-label="下一張環境照片"></button>
  </div>

  {/* 下方官方原始文案 (100% 對齊原頁面，杜絕任何編造內容) */}
  <div class="gp-env-caption-box">
    <h3 class="swiper-small-title">
      {isCn ? "希玛微笑矫视中心位于" : "希瑪微笑矯視中心位於"}
    </h3>
    <p class="swiper-small-text">
      {isCn ? "中环中建大厦、旺角雅兰中心，交通便利易达。" : "中環中建大廈、旺角雅蘭中心，交通便利易達。"}
    </p>
  </div>
</div>

<style lang="scss">
  .gp-env-swiper-wrapper {
    width: 100%;
    margin-top: clamp(24px, 3vw, 40px);
  }

  .gp-env-carousel {
    max-width: 900px;
    margin: 0 auto;
    position: relative;
  }

  // 1. 主輪播圖
  .gp-env-main-swiper {
    width: 100%;
    height: clamp(220px, 35vw, 480px);
    border-radius: clamp(18px, 3vw, 36px);
    overflow: hidden;
    position: relative;
    box-shadow: 0 4px 20px rgba(69, 112, 182, 0.08);

    .gp-env-main-slide {
      width: 100%;
      height: 100%;
      position: relative;
      overflow: hidden;
    }

    .gp-env-main-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
  }

  // 2. 縮略圖 Swiper
  .gp-env-thumbs-swiper {
    width: 100%;
    height: clamp(55px, 8vw, 90px);
    margin-top: clamp(12px, 1.5vw, 20px);
    box-sizing: border-box;

    .gp-env-thumb-slide {
      width: 20%;
      height: 100%;
      border-radius: clamp(10px, 1.5vw, 18px);
      overflow: hidden;
      cursor: pointer;
      opacity: 0.5;
      transition: all 0.3s ease;
      box-sizing: border-box;
      border: 2px solid transparent;

      &.swiper-slide-thumb-active {
        opacity: 1;
        border-color: #4570b6;
        transform: scale(1.02);
      }
    }

    .gp-env-thumb-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
  }

  // 原版經典導航箭頭 (兩側懸浮，垂直精確對齊)
  .gp-env-nav-btn {
    position: absolute;
    width: 27px;
    height: 48px;
    top: 40%;
    transform: translateY(-50%);
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    background-color: transparent;
    border: none;
    cursor: pointer;
    z-index: 10;
    transition: transform 0.3s ease, opacity 0.3s ease;

    &:hover {
      transform: translateY(-50%) scale(1.15);
    }

    &--prev {
      left: -55px;
      background-image: url("https://statichk.cmermedical.com/imgs/2023/03/bcc35245d4a0fe42.png");
    }

    &--next {
      right: -55px;
      background-image: url("https://statichk.cmermedical.com/imgs/2023/03/c1cc233620b64f31.png");
    }

    @media (max-width: 1024px) {
      &--prev {
        left: -35px;
      }

      &--next {
        right: -35px;
      }
    }

    @media (max-width: 767px) {
      width: 18px;
      height: 32px;

      &--prev {
        left: -20px;
      }

      &--next {
        right: -20px;
      }
    }
  }

  // 3. 官方原始說明文字
  .gp-env-caption-box {
    max-width: 860px;
    margin: clamp(24px, 3vw, 36px) auto 0;
    text-align: center;
  }

  .swiper-small-title {
    color: #4570b6;
    text-align: center;
    font-family: var(--font-hk);
    font-size: clamp(16px, 1.8vw, 25px);
    font-weight: 700;
    line-height: 1.4;
    letter-spacing: clamp(1px, 0.4vw, 7.5px);
    margin-bottom: 10px;
  }

  .swiper-small-text {
    color: #6d6e71;
    text-align: center;
    font-family: var(--font-hk);
    font-size: clamp(14px, 1.3vw, 20px);
    font-weight: 400;
    line-height: 1.8;
    letter-spacing: clamp(1px, 0.4vw, 6px);
  }
</style>

<script>
  import Swiper from "swiper";
  import { Navigation, Thumbs, Autoplay } from "swiper/modules";
  import "swiper/css";
  import "swiper/css/navigation";
  import "swiper/css/thumbs";

  function initGroupProfileSwiper() {
    const mainEl = document.getElementById("gpEnvMainSwiper");
    const thumbsEl = document.getElementById("gpEnvThumbsSwiper");
    if (!mainEl || !thumbsEl) return;

    if ((mainEl as any).swiper) {
      (mainEl as any).swiper.destroy(true, true);
    }
    if ((thumbsEl as any).swiper) {
      (thumbsEl as any).swiper.destroy(true, true);
    }

    const thumbsSwiper = new Swiper(thumbsEl, {
      modules: [Thumbs],
      spaceBetween: 12,
      slidesPerView: 5,
      freeMode: true,
      watchSlidesProgress: true,
      breakpoints: {
        320: {
          slidesPerView: 4,
          spaceBetween: 8,
        },
        768: {
          slidesPerView: 5,
          spaceBetween: 12,
        },
      },
    });

    new Swiper(mainEl, {
      modules: [Navigation, Thumbs, Autoplay],
      spaceBetween: 0,
      loop: true,
      autoplay: {
        delay: 4500,
        disableOnInteraction: false,
        pauseOnMouseEnter: true,
      },
      navigation: {
        nextEl: ".gp-env-nav-btn--next",
        prevEl: ".gp-env-nav-btn--prev",
      },
      thumbs: {
        swiper: thumbsSwiper,
      },
    });
  }

  document.addEventListener("DOMContentLoaded", initGroupProfileSwiper);
  document.addEventListener("astro:page-load", initGroupProfileSwiper);
</script>
